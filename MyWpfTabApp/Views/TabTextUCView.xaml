<UserControl x:Class="MyWpfTabApp.Views.TabTextUCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid ScrollViewer.HorizontalScrollBarVisibility="Visible" ScrollViewer.CanContentScroll="True">
        <ScrollViewer Margin="40" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <TextBox TextWrapping="Wrap" Text="{Binding MyText, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"  HorizontalScrollBarVisibility="Visible"/>
        </ScrollViewer>
    </Grid>
</UserControl>
